/* eslint-disable @typescript-eslint/no-var-requires */
// import { buildApp } from '@/main/config'

// import request from 'supertest'

describe('Average Routes', () => {
  test('Disabled tests because they ara making logs', () => {
    expect(1).toBe(1)
  })
  // describe('POST /calculate-average', () => {
  //   test('Should return 200 on success', async () => {
  //     const app = await buildApp()
  //     await request(app)
  //       .post('/calculate-average')
  //       .send({ firstValue: 2, secondValue: 2 })
  //       .expect(200)
  //   })
  //   test('Should return 400 if cep is incorrect', async () => {
  //     const app = await buildApp()
  //     await request(app)
  //       .post('/calculate-average')
  //       .send({ firstValue: 'a', secondValue: 2 })
  //       .expect(400)
  //   })
  // })
})
